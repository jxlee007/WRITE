I'll analyze your comprehensive requirements and create a detailed implementation plan for this AI-Powered Writing Studio. Let me first examine the current codebase to understand what's already in place.

Let me examine the current state more thoroughly to understand what's implemented:

Now I have a comprehensive understanding of your current codebase. Let me analyze what you're asking for and create a detailed implementation plan.

# Comprehensive AI-Powered Writing Studio Implementation Plan

## Current State Analysis

Your existing application already has significant infrastructure:
- ✅ **Authentication**: Clerk integration working
- ✅ **Backend**: Convex database with comprehensive schema (projects, documents, tokens, relationships, generated images, token usage, chat history)
- ✅ **Core Components**: WritingEditor (TipTap), TokenLibrary, ProjectOverview, AIChat, DocumentTree, ProjectManager
- ✅ **Basic Features**: Auto-save, format templates (novel/screenplay/stage_play/comic_script), token mentions (@character), undo/redo, find/replace, export (PDF/DOCX/TXT)
- ✅ **AI Integration**: OpenRouter integration for chat agents

## What You're Actually Requesting

You've provided a specification for a **single HTML file** implementation, but your current project is a **full-stack React/Convex application**. I'll interpret this as wanting to **enhance your existing application** with all the comprehensive features from your specification, rather than creating a separate single-file app.

---

## Implementation Plan: Enhanced AI-Powered Writing Studio

### Phase 1: Enhanced Writing Editor Features (Priority: CRITICAL)

#### 1.1 Color-Coded AI Suggestion System
**Goal**: Real-time AI-powered writing suggestions with visual highlighting and interactive tooltips

**Implementation**:
1. **New TipTap Extension**: `AISuggestion` custom extension
   - Track text ranges that need improvement
   - Apply colored decorations based on suggestion type
   - Store suggestion metadata (type, original, suggested, explanation)

2. **Suggestion Types with Colors**:
   ```typescript
   const SUGGESTION_TYPES = {
     grammar: { color: '#3B82F6', bg: 'rgba(59, 130, 246, 0.25)' },
     spelling: { color: '#EF4444', bg: 'rgba(239, 68, 68, 0.25)' },
     style: { color: '#10B981', bg: 'rgba(16, 185, 129, 0.25)' },
     clarity: { color: '#8B5CF6', bg: 'rgba(139, 92, 246, 0.25)' },
     tone: { color: '#F59E0B', bg: 'rgba(245, 158, 11, 0.25)' },
     consistency: { color: '#6366F1', bg: 'rgba(99, 102, 241, 0.25)' }
   };
   ```

3. **AI Suggestion Generation**:
   - Create Convex action: `generateWritingSuggestions`
   - Use OpenRouter API with specialized prompts
   - Analyze text in chunks (500-word segments)
   - Return structured suggestions: `{ type, range, original, suggested, explanation, confidence }`

4. **Interactive Tooltip System**:
   - React component: `SuggestionTooltip`
   - Position above cursor on hover (500ms delay)
   - Display: badge, original text, suggestion, explanation
   - Actions: Accept (replace text), Reject (dismiss), Modify (edit before applying)

5. **Database Schema Addition**:
   ```typescript
   suggestions: defineTable({
     documentId: v.id("documents"),
     type: v.string(),
     range: v.object({ from: v.number(), to: v.number() }),
     originalText: v.string(),
     suggestedText: v.string(),
     explanation: v.string(),
     status: v.string(), // pending, accepted, rejected
     createdAt: v.number()
   })
   ```

**Files to Create/Modify**:
- `src/components/WritingEditor/AISuggestionExtension.tsx` (new)
- `src/components/WritingEditor/SuggestionTooltip.tsx` (new)
- `convex/suggestions.ts` (new)
- `src/components/WritingEditor.tsx` (modify)

---

#### 1.2 Enhanced Token Mention System with Rich Autocomplete
**Goal**: Transform simple @mentions into rich, context-aware autocomplete with visual previews

**Implementation**:
1. **Enhanced Autocomplete UI**:
   - Display token icon, name, type, and preview description
   - Show recently used tokens at top
   - Filter by context (if writing about location, prioritize location tokens)
   - Fuzzy search matching

2. **Token Preview Card**:
   - Hover over autocomplete item shows full token details
   - Display primary image if available
   - Show related tokens (e.g., character's location, objects they own)

3. **Smart Context Analysis**:
   - Analyze surrounding text before triggering autocomplete
   - If writing dialogue, prioritize character tokens
   - If describing setting, prioritize location tokens

**Files to Modify**:
- `src/components/WritingEditor.tsx` (enhance mention suggestion logic)
- `src/components/TokenPreviewCard.tsx` (create new component)

---

#### 1.3 Format-Specific Auto-Formatting
**Goal**: Intelligent auto-formatting based on screenplay/comic script conventions

**Implementation**:
1. **Screenplay Auto-Formatting**:
   - Detect `INT.` or `EXT.` and auto-capitalize entire line
   - Detect character names (all caps followed by dialogue) and maintain caps
   - Tab key cycles through: Scene → Action → Character → Parenthetical → Dialogue
   - Keyboard shortcuts: Ctrl+1 (Scene), Ctrl+2 (Action), Ctrl+3 (Character), Ctrl+4 (Dialogue)

2. **Comic Script Auto-Formatting**:
   - Auto-detect `PAGE X` and format as page header
   - Auto-detect `PANEL X:` and format as panel description
   - Auto-format `CAPTION:`, `CHARACTER (BALLOON):`, `SFX:`
   - Panel numbering system with visual separators

3. **Stage Play Auto-Formatting**:
   - Auto-format `ACT X` and `SCENE X`
   - Detect `CHARACTER:` pattern and format appropriately
   - Italicize stage directions in brackets/parentheses

4. **Novel Auto-Formatting**:
   - Chapter heading detection and formatting
   - Scene break markers (### or * * *)
   - Auto-indent first line of paragraphs (optional setting)

**Implementation Approach**:
- Create custom TipTap InputRules for each format
- Format-specific keymap handlers
- Add format toolbar with quick-format buttons

**Files to Create/Modify**:
- `src/components/WritingEditor/FormatExtensions.tsx` (new)
- `src/components/WritingEditor/ScreenplayRules.ts` (new)
- `src/components/WritingEditor/ComicScriptRules.ts` (new)
- `src/components/WritingEditor.tsx` (modify - integrate format rules)

---

### Phase 2: AI Integration Enhancements (Priority: HIGH)

#### 2.1 AI Personas with Context-Aware Assistance
**Goal**: Multiple AI personas that provide specialized assistance based on current writing context

**Implementation**:
1. **AI Personas**:
   - Creative Assistant (plot, character, narrative)
   - Technical Editor (grammar, structure, clarity)
   - Format Specialist (industry-standard formatting)

2. **Context-Aware Features**:
   - Analyze current document section
   - Provide relevant suggestions based on content type
   - Track character consistency across document
   - Detect plot holes or timeline issues

3. **Quick Actions**:
   - "Expand this paragraph" button on selection
   - "Improve dialogue" for dialogue sections
   - "Describe this scene visually" for screenplay
   - "Add sensory details" for novel writing

4. **Integration into Editor**:
   - Side panel: AI suggestions appear in real-time
   - Inline suggestions: Highlight text + AI recommendation
   - Command palette: Ctrl+K to invoke AI actions

**Files to Create/Modify**:
- `src/components/AIAssistant/AIPersonaSelector.tsx` (new)
- `src/components/AIAssistant/ContextAnalyzer.ts` (new)
- `src/components/AIAssistant/QuickActions.tsx` (new)
- `convex/aiAssistant.ts` (new)

---

#### 2.2 Enhanced Image Generation with Token Integration
**Goal**: Generate consistent character/location images using token descriptions

**Implementation**:
1. **Token-Based Prompt Composition**:
   - Select multiple tokens from library
   - Auto-compose detailed prompt from token descriptions
   - Include visual consistency seeds
   - Reference primary images for style matching

2. **Integration with Lovable AI**:
   - Use `google/gemini-2.5-flash-image-preview` model
   - Edge function: `generate-token-image`
   - Upload generated images to Convex storage
   - Link images to tokens automatically

3. **Batch Generation**:
   - Generate images for all tokens in project
   - Queue system with progress tracking
   - Variation generation (same prompt, different seeds)

4. **Advanced Settings**:
   - Aspect ratio selector (square, landscape, portrait)
   - Style presets (realistic, anime, watercolor, comic book)
   - Negative prompts for unwanted elements
   - Seed control for consistency

**Files to Create/Modify**:
- `src/components/AIGenerator.tsx` (major overhaul)
- `src/components/TokenImageGenerator.tsx` (new)
- `convex/imageGeneration.ts` (new - replace mock logic)
- Add Lovable AI integration

---

### Phase 3: Collaboration & Version Control (Priority: MEDIUM)

#### 3.1 Version Control System
**Goal**: Track document versions with comparison and restoration

**Implementation**:
1. **Database Addition**:
   ```typescript
   documentVersions: defineTable({
     documentId: v.id("documents"),
     content: v.string(),
     versionNumber: v.number(),
     comment: v.optional(v.string()),
     createdBy: v.string(),
     createdAt: v.number(),
     wordCount: v.number(),
     changesSummary: v.optional(v.string())
   })
   ```

2. **Auto-Versioning Logic**:
   - Create version every 10 saves
   - Create version on format change
   - Manual "Save Version" button with comment

3. **Version History UI**:
   - Side panel: `VersionHistory.tsx`
   - List all versions with timestamps and comments
   - Preview version content
   - Compare versions (diff view)
   - Restore to previous version

4. **Visual Diff Viewer**:
   - Use `diff-match-patch` library
   - Highlight additions (green), deletions (red), changes (yellow)
   - Side-by-side or inline diff modes

**Files to Create/Modify**:
- `src/components/VersionHistory.tsx` (new)
- `src/components/VersionCompare.tsx` (new)
- `convex/documentVersions.ts` (new)
- `src/components/WritingEditor.tsx` (add version controls)

---

#### 3.2 Real-Time Collaboration (COMPLEX - Phase 3B)
**Goal**: Multiple users editing same document simultaneously

**Implementation Options**:
- **Option 1 (Recommended)**: TipTap Collaboration with Yjs
  - Install: `@tiptap/extension-collaboration`, `@tiptap/extension-collaboration-cursor`, `yjs`, `y-websocket`
  - Use Convex Realtime for WebSocket provider
  - Implement presence awareness (show active users)
  - Display user cursors with colored markers

- **Option 2 (Simpler)**: Convex Realtime broadcasting
  - Broadcast changes via Convex subscriptions
  - May have conflicts with simultaneous edits
  - Easier to implement but less robust

**Recommendation**: Implement in Phase 3B after core features are stable

**Files to Create** (if implemented):
- `src/components/WritingEditor/CollaborationExtension.tsx`
- `src/components/Collaboration/PresenceIndicators.tsx`
- `convex/collaboration.ts`

---

### Phase 4: Analytics & Insights (Priority: MEDIUM)

#### 4.1 Real-Time Writing Metrics
**Goal**: Track and display writing metrics in real-time

**Implementation**:
1. **Real-Time Stats** (already partially implemented, enhance):
   - Word count (current + target)
   - Character count (with/without spaces)
   - Reading time estimate
   - Writing velocity (words per minute)
   - Sentence count, paragraph count
   - Average sentence length

2. **Session Analytics**:
   - Time tracking per session
   - Daily/weekly writing streak
   - Productivity graphs (Chart.js or Recharts)
   - Goal tracking (daily word count goals)

3. **Content Analysis**:
   - Readability score (Flesch-Kincaid)
   - Sentiment analysis (positive/negative tone)
   - Keyword density (most frequent words)
   - Character usage frequency (from token mentions)

4. **Dashboard Component**:
   - Visual charts for productivity trends
   - Heatmap calendar for writing activity
   - Token usage statistics
   - Document progress bars

**Files to Create/Modify**:
- `src/components/Analytics/WritingDashboard.tsx` (new)
- `src/components/Analytics/SessionTracker.tsx` (new)
- `src/components/Analytics/ContentAnalyzer.ts` (new)
- `convex/analytics.ts` (new)
- `src/components/StatusBar.tsx` (enhance with more metrics)

---

### Phase 5: Multimedia Integration (Priority: LOW-MEDIUM)

#### 5.1 Image Support in Editor
**Goal**: Insert and manage images within documents

**Implementation**:
1. **Image Insertion**:
   - Drag-drop images into editor
   - Click to browse local files
   - Paste from clipboard (Ctrl+V)

2. **Image Storage**:
   - Upload to Convex storage
   - Generate thumbnails for performance
   - Store image metadata (filename, size, dimensions)

3. **Image Management**:
   - Caption support (text below image)
   - Alt-text for accessibility
   - Positioning options (inline, left/right float, center)
   - Resize handles
   - Media library with all project images

4. **TipTap Image Extension**:
   - Custom image node with caption support
   - Drag-to-resize functionality
   - Image gallery view

**Files to Create/Modify**:
- `src/components/WritingEditor/ImageExtension.tsx` (new)
- `src/components/MediaLibrary.tsx` (new)
- `convex/media.ts` (new)

---

#### 5.2 Audio Recording & Transcription
**Goal**: Record voice notes and transcribe to text

**Implementation**:
1. **Voice Recording**:
   - Browser MediaRecorder API
   - Waveform visualization (wavesurfer.js)
   - Record/stop/play controls
   - Save as MP3/WAV

2. **Transcription** (if Lovable AI supports):
   - Use speech-to-text API
   - Auto-punctuation and formatting
   - Edit transcription with audio sync
   - Insert transcription into document

3. **Audio Storage**:
   - Upload to Convex storage
   - Link to documents/tokens
   - Audio library for playback

**Files to Create**:
- `src/components/VoiceRecorder.tsx` (new)
- `src/components/AudioTranscription.tsx` (new)
- `convex/audio.ts` (new)

---

### Phase 6: Export & Polish (Priority: MEDIUM)

#### 6.1 Enhanced Export System
**Goal**: Professional-grade export in multiple formats

**Current State**: Basic export exists (PDF, DOCX, TXT)

**Enhancements**:
1. **PDF Export Improvements**:
   - Proper pagination with headers/footers
   - Table of contents generation
   - Embedded images and formatting
   - Custom cover page

2. **Word Export (.docx)**:
   - Preserve all formatting (bold, italic, headings)
   - Embedded images
   - Comments as Word comments
   - Track changes compatibility

3. **Format-Specific Exports**:
   - **Screenplay**: Final Draft (.fdx), Fountain format
   - **Comic Script**: Celtx format
   - **Novel**: EPUB format for e-readers

4. **HTML/Web Export**:
   - Responsive web page
   - Embedded CSS styling
   - Shareable link generation

**Files to Modify**:
- `src/components/ExportMenu.tsx` (major enhancement)
- Add libraries: `docx`, `epub-gen`, `fountain-js`

---

#### 6.2 Settings & Customization
**Goal**: User preferences and customization options

**Implementation**:
1. **Settings Categories**:
   - **Account**: Profile, email, password
   - **Editor**: Font family, font size, line height, theme
   - **Auto-Save**: Interval, enable/disable
   - **AI**: Default model, quality presets, suggestion sensitivity
   - **Export**: Default format preferences
   - **Keyboard Shortcuts**: Customize shortcuts

2. **Settings Storage**:
   - Store in Convex user profile
   - Local storage for offline access
   - Sync across devices

3. **Settings UI**:
   - Tabbed dialog component
   - Preview changes in real-time
   - Reset to defaults button

**Files to Create**:
- `src/components/Settings/SettingsDialog.tsx` (new)
- `src/components/Settings/EditorSettings.tsx` (new)
- `src/components/Settings/AISettings.tsx` (new)
- `convex/userSettings.ts` (new)

---

#### 6.3 Global Search & Keyboard Shortcuts
**Goal**: Quick navigation and actions across the application

**Implementation**:
1. **Global Search** (Ctrl+K command palette):
   - Search across all documents
   - Search in token library
   - Search in image prompts
   - Fuzzy search with instant results
   - Quick actions (create document, create token, etc.)

2. **Keyboard Shortcuts**:
   - Document shortcuts: Ctrl+S (save), Ctrl+N (new), Ctrl+E (export)
   - Editor shortcuts: Already implemented (bold, italic, undo/redo)
   - Navigation: Ctrl+P (command palette), Ctrl+Tab (switch documents)
   - Format shortcuts: Ctrl+1-6 (headings), Ctrl+Shift+F (format-specific)

3. **Shortcut Help**:
   - Keyboard shortcuts overlay (Ctrl+/)
   - Tooltips show shortcuts on hover
   - Customizable shortcuts in settings

**Files to Create**:
- `src/components/GlobalSearch.tsx` (new)
- `src/components/KeyboardShortcuts.tsx` (new)
- `src/hooks/useKeyboardShortcuts.ts` (new)

---

### Phase 7: AI Comic Factory Integration (ADVANCED - Optional)

**Goal**: Generate comic panels from comic script text

**Research Needed**: Hugging Face AI Comic Factory API availability

**Implementation** (if API available):
1. **Comic Panel Parser**:
   - Parse comic script format
   - Extract PAGE, PANEL, CAPTION, DIALOGUE, SFX
   - Structure data for API

2. **Panel Generation**:
   - Send panel descriptions to AI Comic Factory
   - Maintain consistent art style
   - Generate sequential pages

3. **Comic Preview**:
   - Split view: script on left, panels on right
   - Panel-by-panel regeneration
   - Drag-drop to reorder panels
   - Export as PDF or CBZ

**Files to Create**:
- `src/components/ComicPanelEditor.tsx` (new)
- `src/components/ComicPreview.tsx` (new)
- `convex/comicGeneration.ts` (new)

---

## Implementation Priority Order

### Sprint 1 (Week 1-2): Foundation
1. ✅ Ensure authentication and basic editor work perfectly
2. 🎯 Implement color-coded AI suggestion system (Phase 1.1)
3. 🎯 Enhanced token mention with rich autocomplete (Phase 1.2)
4. 🎯 Format-specific auto-formatting (Phase 1.3)

### Sprint 2 (Week 3-4): AI Power
1. 🎯 AI personas with context-aware assistance (Phase 2.1)
2. 🎯 Enhanced image generation with token integration (Phase 2.2)
3. 🎯 Real-time writing metrics and analytics (Phase 4.1)

### Sprint 3 (Week 5-6): Collaboration & Export
1. 🎯 Version control system (Phase 3.1)
2. 🎯 Enhanced export system (Phase 6.1)
3. 🎯 Settings & customization (Phase 6.2)
4. 🎯 Global search and keyboard shortcuts (Phase 6.3)

### Sprint 4 (Week 7-8): Multimedia & Polish
1. 🎯 Image support in editor (Phase 5.1)
2. 🎯 Audio recording & transcription (Phase 5.2)
3. 🎯 Performance optimization for large documents
4. 🎯 Accessibility improvements (ARIA labels, keyboard navigation)

### Sprint 5 (Week 9+): Advanced Features (Optional)
1. 🎯 Real-time collaboration with Yjs (Phase 3.2)
2. 🎯 AI Comic Factory integration (Phase 7)
3. 🎯 Mobile responsiveness
4. 🎯 GitHub prompt templates integration

---

## Technical Architecture Decisions

### 1. AI Integration Strategy
- **Primary**: OpenRouter API (already integrated)
- **Image Generation**: Lovable AI (`google/gemini-2.5-flash-image-preview`)
- **Suggestion Engine**: OpenRouter with specialized prompts
- **Cost Management**: Track token usage, implement caching

### 2. Real-Time Features
- **Backend**: Convex (already in use)
- **Real-Time Sync**: Convex subscriptions
- **Collaboration**: TipTap Collaboration + Yjs (if implemented)

### 3. Storage Strategy
- **Documents**: Convex database
- **Images**: Convex file storage
- **Audio**: Convex file storage
- **User Settings**: Convex database + localStorage

### 4. Performance Optimization
- **Large Documents**: Virtual scrolling for document list
- **Image Optimization**: Lazy loading with thumbnails
- **Code Splitting**: Dynamic imports for heavy components
- **Debouncing**: Search inputs, auto-save triggers

### 5. UI/UX Framework
- **Component Library**: Radix UI + shadcn (already in use)
- **Styling**: Tailwind CSS (already in use)
- **Dark Theme**: Already implemented, maintain consistency
- **Responsive**: Desktop-first, tablet-compatible

---

## Key Differences from Your Specification

Your spec requested a **single HTML file**, but your project is a **full-stack React application**. I'm adapting the requirements to enhance your existing architecture:

### What We're Keeping from Your Spec:
✅ All feature functionality (suggestions, tokens, formats, AI, analytics)
✅ Dark theme optimized for writing
✅ Professional UI/UX patterns
✅ Sample data structures
✅ Color-coded suggestion system
✅ Real-time metrics
✅ Multimedia support

### What We're Adapting:
🔄 **Architecture**: Full-stack React/Convex instead of single HTML
🔄 **Storage**: Convex database instead of localStorage
🔄 **Authentication**: Clerk (already integrated) instead of basic auth
🔄 **AI**: OpenRouter + Lovable AI instead of mock responses
🔄 **Deployment**: Existing deployment infrastructure

### Why This Approach is Better:
- ✅ Scalable to thousands of users
- ✅ Real backend with data persistence
- ✅ Authentication and security built-in
- ✅ Real AI integration (not simulated)
- ✅ Collaboration-ready architecture
- ✅ Professional development workflow

---

## Validation Criteria (from your spec)

✅ **Professional writer can use daily**: Yes - comprehensive feature set
✅ **AI suggestions provide genuine value**: Yes - real AI integration
✅ **Token system feels natural**: Yes - enhanced autocomplete
✅ **Export functions produce publication-ready documents**: Yes - enhanced exports
✅ **Interface feels polished**: Yes - maintaining design system

---

## Next Steps

After you approve this plan, I'll implement features in priority order:

1. **Phase 1**: Enhanced editor with AI suggestions (most impactful)
2. **Phase 2**: AI enhancements and image generation
3. **Phase 3**: Version control and exports
4. **Phase 4**: Analytics and multimedia
5. **Phase 5**: Advanced features (collaboration, comic factory)

Each phase will be fully functional before moving to the next.

Start with Phase 1: Enhanced Editor
Start with Phase 2: AI Enhancements
Implement Complete Plan